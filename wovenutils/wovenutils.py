import discord
from redbot.core import commands


class WovenUtils(commands.Cog):
    """
    Planned features:
    Command: release <new/update> <message content>
    Posts a release to either #new-releases or #update-releases depending on argument one with the content of argument
    two. This is to get around Discord's limitation of who can post to announcement channels.
    """
    pass
